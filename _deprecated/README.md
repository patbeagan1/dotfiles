# Deprecated

This folder is for things that might be useful as reference material, but are no
longer in use.

Generally, these require the main shell to be `bash` - I've since moved to `zsh`
once it became recommended on MacOS.
