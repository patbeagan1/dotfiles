# Personal utilities tree for jan (canonical under dotfiles/jan).
# Categories include per-command YAML under scripts/<category>/.
about: 'Personal utilities (categorized; each command is a YAML include).'
commands:
  git:
    include: git.yaml
  android:
    include: android.yaml
  docker:
    include: docker.yaml
  network:
    include: network.yaml
  media:
    include: media.yaml
  files:
    include: files.yaml
  text:
    include: text.yaml
  tmux:
    include: tmux.yaml
  time:
    include: time.yaml
  system:
    include: system.yaml
  misc:
    include: misc.yaml
