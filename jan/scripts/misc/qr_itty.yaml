about: Converted from aliases/alias_misc.zsh (`qr_itty`).
commands:
  help:
    about: Describe this script.
    exec:
      text: |
        qr_itty

        Converted from aliases/alias_misc.zsh (`qr_itty`).

  run:
    about: 'Run the linked script; forwards args.'
    exec:
      zsh: |
        function qr_itty () { qr $(itty.sh "$1"); }

        qr_itty "$@"

      passthrough: true
