# Personal utilities tree for jan (canonical under dotfiles/jan).
# Each command is a YAML include under scripts/tmux/.
about: tmux utilities
commands:
  tmux-extend-column:
    include: scripts/tmux/tmux-extend-column.yaml
